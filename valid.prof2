function printint(m: integer): integer {
    return m;
}

function Main(): integer {
	var j, k, m, n: integer;
	var x: intarray[19];
	var b: boolean;

	function foo(inc: integer): integer {
		m = m + inc;
		return 1;
	}

	j = 2;
	k = - j;
	m = 100 - k * k;
	x[1] = foo( 1 );
	n = printint( m );
	x[1] = 1;
 	return x[7+1];
}
